# DevInsight 后端配置文件
# 复制本文件为 config.env 并填入实际值: cp config.env.example config.env

# SQLite 数据库文件路径（绝对路径）
DEVINSIGHT_DB=/your/path/to/data/devinsight.db

# 服务端口
PORT=8080

# GitHub Personal Access Token（用于从 GitHub API 同步仓库数据）
GITHUB_TOKEN=ghp_xxxxxxxxxxxxxxxxxxxx

# ---- 2.4 AI 模块配置（可选，不配则 AI 问答不可用）----
# LLM API 地址（兼容 OpenAI Chat Completions 格式）
# OpenAI:    https://api.openai.com
# DeepSeek:  https://api.deepseek.com
# 阿里通义:  https://dashscope.aliyuncs.com/compatible-mode/v1
LLM_API_BASE=https://api.openai.com

# LLM API Key
LLM_API_KEY=sk-xxxxxxxxxxxxxxxxxxxx

# 模型名称
LLM_MODEL=gpt-3.5-turbo

# ---- Embedding 向量检索配置（可选，不配则仅使用关键词检索）----
# Embedding API 地址（兼容 OpenAI Embeddings 格式）
# 默认复用 LLM_API_BASE，如需单独配置可取消注释
# EMBEDDING_API_BASE=https://dashscope.aliyuncs.com/compatible-mode/v1
# EMBEDDING_API_KEY=sk-xxxxxxxxxxxxxxxxxxxx
# Embedding 模型名称
EMBEDDING_MODEL=text-embedding-v3

# Embedding 批量生成失败时的重试参数（建议保留默认）
# 常见失败原因是网络/SSL 抖动或服务端限流；重试可显著降低漏生成
EMBEDDING_RETRY_TIMES=3
EMBEDDING_RETRY_BACKOFF_MS=500
EMBEDDING_RETRY_BACKOFF_MAX_MS=4000

# ---- 2.2 代码质量分析配置（可选）----
# 外部工具路径（未配置时默认使用 PATH 中的 clang-tidy/cppcheck）
CLANG_TIDY_BIN=clang-tidy
CPPCHECK_BIN=cppcheck

# clang-tidy 需要 compile_commands.json 所在目录
# 可设置为 repo 的 build 目录或生成 compile_commands.json 的目录
CLANG_TIDY_COMPILE_COMMANDS=E:/study/SoftwareLab/lab/se-metrics-platform/backend/data/repo_cache/19/build

# clang-tidy 额外参数（可选）
CLANG_TIDY_ARGS=

# 静态分析输出目录（cppcheck xml 输出）
QUALITY_OUTPUT_DIR=data/quality
