# 更新记录 — 2026-05-17

## 概要
今天完成针对“系统日志”功能的前端与后端若干改进，主要目标是让系统自身运行日志更易于查看与分析，包括：操作日志（Operation）与 AI 用量（AI Usage）。

## 完成的功能
- 前端：新增/改造 “系统日志” 页面（表格视图），实现：
  - 表格化展示每条日志（每行为一个记录元组）；
  - 分页（每页可选 10/20/50）、上一/下一页控制（客户端分页）；
  - 时间范围筛选（开始/结束，`datetime-local` 输入），发送为 ISO UTC 给后端；
  - 每行可展开查看 JSON 详情（操作日志显示 `detail` 字段，AI 日志显示整条记录）；
  - 样式与响应式调整。

- 后端：新增/修改了用于记录与查询系统日志的相关逻辑（包括 AI 用量计量与统计接口），并修复/兼容数据库迁移问题，使服务能在旧 schema 上平滑启动。

## 修改的文件
- [backend/CMakeLists.txt](backend/CMakeLists.txt): +1 / -0
- [backend/src/ai/ai_assistant.cpp](backend/src/ai/ai_assistant.cpp): +66 / -2
- [backend/src/ai/ai_assistant.h](backend/src/ai/ai_assistant.h): +5 / -0
- [backend/src/api/routes_ai.cpp](backend/src/api/routes_ai.cpp): +55 / -0
- [backend/src/api/routes_get.cpp](backend/src/api/routes_get.cpp): +204 / -0
- [backend/src/api/routes_post.cpp](backend/src/api/routes_post.cpp): +22 / -1
- [backend/src/common/system_log.h](backend/src/common/system_log.h): +30 / -0
- [backend/src/common/system_log.cpp](backend/src/common/system_log.cpp): +80 / -0
- [backend/src/db/db.cpp](backend/src/db/db.cpp): +33 / -2
- [frontend/src/App.vue](frontend/src/App.vue): +93 / -13
- [frontend/src/main.js](frontend/src/main.js): +2 / -0
- [frontend/src/views/SystemLogsView.vue](frontend/src/views/SystemLogsView.vue): +423 / -1


> 说明：以上为当前工作区相对于 `git` HEAD 的未提交差异统计（`git diff --numstat` 输出）。如果需要我也可以生成完整的 `git diff` 文件或把这些改动提交为一个带说明的 commit。

## 主要变更点（更详尽）
- 前端 `[frontend/src/views/SystemLogsView.vue]`：页面改造为表格（包含分页、时间筛选、详情展开、样式调整）。
- 前端 `[frontend/src/App.vue]` / `[frontend/src/main.js]`：路由与侧边栏相关调整，新增“系统日志”入口（以便访问新页面）。
- 后端：
  - 在 AI 调用流程中记录 token 使用与耗时，计算 cost 并写入 AI 用量日志；
  - 在关键路由（仓库创建、同步、commit_files、AI 问答）处记录操作日志；
  - 新增查询接口：操作日志列表、AI 用量列表、以及“今日统计”（按北京时间口径）。
  - DB 初始化/迁移增强以兼容旧 schema，避免启动失败。


